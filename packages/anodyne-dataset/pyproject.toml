[project]
name = "anodyne-dataset"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["anodyne-core", "pydantic>=2.8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
anodyne-core = { workspace = true }
